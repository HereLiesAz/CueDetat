<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      DEBUG-ONLY overrides.
      Allows cleartext to the local Myriad backend (emulator loopback at 10.0.2.2,
      and localhost for instrumented tests). Release builds use the stricter
      configuration in src/main/res/xml/network_security_config.xml.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
