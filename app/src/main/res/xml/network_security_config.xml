<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Release-build network policy.
      Cleartext is forbidden for every domain. The dev-only cleartext exception
      for 10.0.2.2 / localhost lives in src/debug/res/xml/network_security_config.xml
      and only applies to debug builds.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
