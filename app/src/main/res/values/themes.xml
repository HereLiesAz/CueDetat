<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- For Material 3 components like FloatingActionButton, prefer a Material3 theme -->
    <style name="Theme.PoolProtractor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_black</item>
         <!-- M3 uses colorPrimaryContainer, etc. -->
        <item name="colorOnPrimary">@color/app_yellow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/app_black</item>
         <!-- M3 uses colorSecondaryContainer, etc. -->
        <item name="colorOnSecondary">@color/app_yellow</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/app_yellow</item> <!-- Or a specific color -->
        <!-- Customize your theme here. -->
    </style>
</resources>