<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    featureSplit="feature_expert_ar"
    package="com.hereliesaz.cuedetat.feature.expert.ar"
    android:versionCode="561"
    android:versionName="1.10.3.561" >

    <uses-sdk android:targetSdkVersion="37" />

    <!--
      On-demand delivery: the Expert-AR split is NOT installed with the base app.
      The base requests it at runtime via SplitInstallManager (PlayArFeatureDelivery),
      gated on entitlement, the first time an entitled user enters Expert AR.

      dist:fusing include="true" keeps the module available in fused/standalone
      APKs (universal APK / bundletool standalone) so non-Play distributions of
      the bundle still contain the AR flow.
    -->
    <dist:module
        dist:instant="false"
        dist:title="@string/expert_ar_feature_title" >
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <queries>
        <package android:name="com.google.ar.core" />
        <package android:name="com.android.vending" />

        <intent>
            <action android:name="com.google.android.play.core.install.BIND_INSTALL_SERVICE" />
        </intent>
    </queries>

    <application
        android:debuggable="true"
        android:extractNativeLibs="false" > <!-- The minimal version code of ARCore APK required for an app using this SDK. -->
        <meta-data
            android:name="com.google.ar.core.min_apk_version"
            android:value="260760000" /> <!-- This activity is critical for installing ARCore when it is not already present. -->
        <activity
            android:name="com.google.ar.core.InstallActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:splitName="feature_expert_ar"
            android:theme="@android:style/Theme.Material.Light.Dialog.Alert" />
    </application>

</manifest>