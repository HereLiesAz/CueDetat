# Cue d'État — ignore rules.
#
# This file was 2,947 lines and 290 KB. Almost all of it was one enumerated path
# per generated file: thousands of /opencv/java/javadoc/*.html entries from a
# vendored SDK that is no longer in the repository, per-session Kotlin compiler
# .salive files, and one line for every timestamped context dump an agent had
# ever dropped in the working tree. Patterns, not an inventory.

# ── Gradle / build output ────────────────────────────────────────────────────
.gradle/
build/
*/build/
**/build/
captures/
.externalNativeBuild/
.cxx/
*.log

# ── Local configuration & secrets ────────────────────────────────────────────
local.properties
*.jks
*.keystore
!debug.keystore
google-services.json
secrets.properties

# ── Kotlin ───────────────────────────────────────────────────────────────────
.kotlin/

# ── IDE ──────────────────────────────────────────────────────────────────────
*.iml
.idea/
!.idea/codeStyles/
!.idea/inspectionProfiles/
.vscode/
*.swp
*~

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Node (docs tooling) ──────────────────────────────────────────────────────
node_modules/

# ── Agent scratch ────────────────────────────────────────────────────────────
# Timestamped context/backup dumps. These used to be listed one line at a time,
# which is why this file grew by a line per session.
project_context_for_ai_*
project_backup_full_*
project_source_only_*
project_source_scoped_*
.superpowers/

# ── Python (CI tooling) ──────────────────────────────────────────────────────
__pycache__/
*.pyc
app/release/app-armeabi-v7a-release.apk
app/release/app-x86-release.apk
app/release/app-x86_64-release.apk
app/release/baselineProfiles/0/app-arm64-v8a-release.dm
app/release/baselineProfiles/0/app-armeabi-v7a-release.dm
app/release/baselineProfiles/0/app-x86-release.dm
app/release/baselineProfiles/0/app-x86_64-release.dm
app/release/baselineProfiles/1/app-arm64-v8a-release.dm
app/release/baselineProfiles/1/app-armeabi-v7a-release.dm
app/release/baselineProfiles/1/app-x86-release.dm
app/release/baselineProfiles/1/app-x86_64-release.dm
app/release/output-metadata.json
/app/release/app-release.aab
