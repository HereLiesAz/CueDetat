<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <!--
      On-demand delivery: the model split is NOT installed with the base app.
      The base requests it at runtime via SplitInstallManager (ModelModuleManager)
      the first time ML detection is needed.

      dist:fusing include="true" keeps the module available in fused/standalone
      APKs (universal APK / bundletool standalone) so non-Play distributions of
      the bundle still contain the model.
    -->
    <dist:module
        dist:instant="false"
        dist:title="@string/mlmodel_feature_title">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application />
</manifest>
